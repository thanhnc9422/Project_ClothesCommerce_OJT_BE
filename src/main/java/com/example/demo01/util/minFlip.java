package com.example.demo01.util;

import java.util.ArrayList;
import java.util.Arrays;

public class minFlip {
    public static void main(String[] args) {
       String s = "010111000110";
       String[] bits = s.split("");
       while (true){
           String[] b = bits;
           ArrayList<String> bitsList = new ArrayList<>(Arrays.asList(bits));
           if(bits[0] != bits[1] && bits[bits.length-1] != bits[bits.length-2]){

           }
       }

    }
}
